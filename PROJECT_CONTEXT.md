# PROJECT_CONTEXT.md — MuscleHubEG

> **Last updated:** 2026-08-19
> **Audience:** Any AI agent or human contributor starting work on
> MuscleHubEG. Read this BEFORE touching code.
> **Companion files:** `AGENTS.md` (operating rules), `SECURITY.md`
> (security policy), `DEVELOPER_GUIDE.md` (architecture + setup).

---

## 1. Product Identity

**MuscleHubEG** (publicly branded **MuscleHub**) is a bilingual
(Arabic / English) fitness, nutrition, and coaching platform. The
primary audience is the Egyptian / Gulf Arabic-speaking market, with
English as the secondary interface language.

The product offers:

- A library of **868+ exercises** (sourced from the open-source
  `yuhonas` exercise dataset, images hosted on GitHub).
- A food database of **8,830+ foods** with per-100g macros.
- **6 free tools**: Calorie calculator, BMI, Macros, Body-Fat %,
  Water tracker, Meal Planner.
- **EVO AI Coach** — a floating chat widget powered by OpenRouter's
  free model tier, with a local rule-based fallback.
- A **blog CMS** with automated AI article generation (3-step cron
  pipeline) and manual coach-driven generation.
- A **coach dashboard** for client management, payment review
  (InstaPay / Vodafone Cash receipts), and support tickets.
- **4 membership tiers**: Free, Premium ($14.99/mo), Pro ($29.99/mo),
  Coaching ($39.99/mo). Multiple memberships are allowed (e.g. a
  client can hold Coaching + Premium simultaneously — migration 0011).
- A **referral system** with 20% commission tracking.
- PWA support (manifest + service worker).

Live deployment: `https://musclehubeg.vercel.app` (Vercel, `sin1`
region).

---

## 2. Public Repository ≠ Open-Source Product

This is the most important distinction for any agent or contributor
to internalize:

- **The Git repository is public.** Anyone can browse, clone, fork,
  and read the code at
  `https://github.com/muscleshubfit-cpu/musclehubeg`. This is
  intentional — it enables agent-assisted collaboration, transparent
  auditing, and faster iteration.
- **The product is proprietary.** The code is NOT licensed as open
  source. The `LICENSE` file in the repository root is an
  all-rights-reserved proprietary license. You may NOT redistribute
  the code, host a competing service, or remove the license notice.
- **Do not make the repository private.** The public-visibility
  decision is a deliberate business choice by the owner. Agents must
  not change this setting.
- **Public ≠ free to use in production.** Running this codebase
  requires the owner's Supabase project, OpenRouter keys, and Vercel
  deployment — none of which are included in the repository.

---

## 3. Proprietary Software Status

The source code, the database schema, the AI prompts, the blog
content, the exercise dataset curation, the food database curation,
the UI design, the brand name "MuscleHub" / "MuscleHubEG", the EVO
coach persona, and all marketing copy are **proprietary intellectual
property** of the project owner (`muscleshubfit@gmail.com`).

Exceptions:

- The raw `yuhonas` exercise dataset is MIT-licensed — attribution is
  preserved in `src/lib/exercises.ts` header comment.
- Open-source dependencies retain their original licenses (see
  `package.json` for the full list).
- Content generated by AI providers (blog articles) is owned by the
  project owner per the provider ToS.

See `LICENSE` for the full proprietary terms.

---

## 4. Current Architecture Summary

### 4.1 Stack

| Layer | Technology | Notes |
|---|---|---|
| Framework | Next.js 16 (App Router) | `^16.1.1` in `package.json` |
| UI Library | React 19 | `^19.0.0` |
| Styling | Tailwind CSS 4 | PostCSS plugin, not `tailwind.config.js` import |
| UI Primitives | shadcn/ui (new-york style) | `components.json` — `rsc: true`, `tsx: true` |
| Backend | Supabase (Postgres + Auth + Storage + RLS) | `@supabase/ssr` cookie-based |
| AI | OpenRouter (free tier) | 6-model fallback chain + `Promise.any` race |
| Charts | Recharts 3 | Lazy-loaded |
| Forms | react-hook-form + zod | |
| Analytics | Vercel Analytics + GA4 | Optional via env vars |
| Ads | Google AdSense | Tier-gated, suppressed on auth routes |
| Deployment | Vercel (Singapore region) | `vercel.json` |
| Runtime | Bun (dev + Vercel install) | `bun install` in `vercel.json` |

### 4.2 Layers (top-down)

```
Pages (App Router — server + client components)
   ↓
Views (src/components/views/ — page-level UI, 23 files)
   ↓
Components (src/components/ — reusable UI, includes 50 shadcn files)
   ↓
Hooks (src/hooks/ — useAuth, useNav, useMembershipTier, ...)
   ↓
Data Layer (src/lib/data.ts — Supabase CRUD + localStorage fallback)
   ↓
Supabase (Postgres + Auth + Storage + RLS)
```

### 4.3 Key Architectural Decisions

1. **Dual-mode data layer**: `src/lib/data.ts` works against Supabase
   in production, and falls back to `localStorage` "demo mode" when
   Supabase env vars are missing. This lets the UI be exercised
   without a backend.
2. **Cookie-based auth via middleware**: `src/middleware.ts` uses
   `@supabase/ssr`'s `createServerClient` to sync the PKCE verifier
   to cookies so the server-side `/auth/callback` route can exchange
   the OAuth code. Without this middleware, Google OAuth fails.
3. **Server-side auth helpers**: `src/lib/auth-server.ts` exposes
   `requireUser` / `requireCoach` that read the session from cookies
   and load the user's profile. In demo mode they return `null`.
4. **Client-side tier resolution**: `useMembershipTier(profile)`
   queries the `subscriptions` table and picks the highest-priority
   active membership.
5. **AI provider abstraction**: `src/lib/ai-provider.ts` is the
   single entry point for OpenAI-compatible providers (OpenRouter,
   OpenAI, Gemini, Anthropic, Groq, DeepSeek). Switching providers
   is a config change, not a code change.
6. **Two AI call paths**:
   - `callFreeOpenRouter()` — sequential fallback, used for plans /
     articles / research.
   - `callFreeOpenRouterRace()` — parallel `Promise.any`, used for
     chat and swap (3-8s instead of 18-25s).
7. **No external PDF library**: `src/lib/result-png-export.ts` uses
   Canvas 2D → JPEG → minimal PDF 1.4 — keeps the bundle lean.
8. **i18n without a framework**: `src/lib/i18n.tsx` is a custom
   context provider, no `next-intl` / `react-i18next` dependency.
   The `/ar/*` route group mirrors the English routes.

### 4.4 Repository Statistics (verified 2026-08-19)

| Metric | Value | Source |
|---|---|---|
| TypeScript / TSX files in `src/` | 226 | `find src -name "*.ts" -o -name "*.tsx" \| wc -l` |
| Pages (`page.tsx`) | 47 | `find src/app -name "page.tsx" \| wc -l` |
| API routes | 28 | `find src/app/api -name "route.ts*" \| wc -l` |
| shadcn UI components | 50 | `find src/components/ui -name "*.tsx" \| wc -l` |
| Views (`src/components/views/`) | 23 | `find src/components/views -name "*.tsx" \| wc -l` |
| Database migrations | 12 | `0001` → `0012` in `supabase/migrations/` |
| Tables defined via migrations | 20 | unique `CREATE TABLE` across migrations |
| Tables referenced in code but NOT in migrations | 3 | `plan_swaps`, `progress_photos`, `coach_presence` — created in production via ad-hoc SQL Editor (Phase 5) |
| Exercises dataset | 870 entries | `grep -c "slug:" src/lib/exercises.ts` |
| Foods dataset | 8,832 entries | `grep -c "slug:" src/lib/foods.ts` |
| Test files (`.test.ts` / `.spec.ts`) | 0 | no test framework configured |

---

## 5. Current Development Phase

The project is in a **post-launch stabilization phase**. The
production site is live at `https://musclehubeg.vercel.app`, has been
through one Phase-1 QA pass (40 user flows + 7 API endpoints, manual)
and one Phase-5 deep QA pass on production (63 curl checks + browser
flows + a real demo client account). Phase 6 delivered AI speed-ups.

Phases summary:

| Phase | Date | Theme | Status |
|---|---|---|---|
| Phase 1–4 | pre-2026-08-19 | Initial build + bug fixes (B1–B17) | Done |
| Phase 5 | 2026-08-19 | Production QA: critical DB fixes (C1–C5) | Done |
| Phase 6 | 2026-08-19 | AI speed-up: chat 5-7x faster, chunked article gen | Done |
| **Current** | 2026-08-19 | **Documentation + governance hardening (this task, MH-DOC-001)** | In progress |

---

## 6. Current Known Issues

A reconciled, evidence-based list. (Previous docs claimed "100% done"
based on smoke tests passing; the actual state is more nuanced — see
`PROGRESS.md` for the full list with status flags.)

### Critical (blocks user-facing flow)

| ID | Issue | Status |
|---|---|---|
| C5 | EVO AI may still fall back to local-only replies if `OPENROUTER_API_KEY` is not set in the Vercel project env vars | Code path fixed (race + cleanup); config-dependent |
| C6 | Vercel project may or may not be auto-deploying from `main` — agent did not verify (no Vercel API token in scope) | Unverified — owner must confirm in Vercel dashboard |

### High priority (UX / localization)

| ID | Issue | Status |
|---|---|---|
| H1 | Root `<html>` tag has hardcoded `lang="en" dir="ltr"` — affects Arabic `/ar/*` routes' crawler interpretation | Verified in `src/app/layout.tsx:29`. Mitigated via `Content-Language` header in middleware. |
| H2 | `MEMBERSHIPS[].features` arrays are Arabic-only — English users see Arabic text on the memberships page | Verified in `src/lib/memberships.ts`. Not fixed. |
| H3 | `PlansView` contains hardcoded Arabic strings (e.g. "تبديل الوجبات اليوم: 2/2 متبقي") even in English mode | Verified — not fixed. |
| H4 | i18n keys `prog.uploadPhoto`, `prog.photos`, `prog.noPhotos` are missing from `src/lib/i18n.tsx` | Verified — keys do not exist. Not fixed. |
| H5 | Some blog posts may still have `author = 'Ahmed Zake'` in the `blog_posts` table | DB-side — not verifiable without Supabase access. Not fixed. |
| H6 | `/ar/exercises` and `/ar/foods` return 404 — no Arabic mirror pages exist for these routes | Verified — `src/app/ar/` only contains `page.tsx`, `layout.tsx`, `blog/page.tsx`, `blog/[slug]/page.tsx`. Not fixed. |

### Medium priority (cleanup)

| ID | Issue | Status |
|---|---|---|
| M1 | Newsletter "Subscribe to our newsletter" copy may still appear in some tool pages | Not verified in this pass. |
| M2 | `/coach`, `/coach/payments`, `/coach/support` render the dashboard view for non-coaches instead of redirecting | Verified — `src/app/(app)/coach/page.tsx` falls through to `<DashboardView />` when `!isCoach`. Not fixed. |
| M3 | Sitemap may contain a duplicate URL: `/blog/best-protein-powder-muscle-growth-copy-msn3h2hm` | Not verified in this pass. |
| M4 | Profile page stat card shows "4 Tools" instead of "6 Tools" (5 calculators + meal planner) | Verified — `src/app/profile/page.tsx:153`. Not fixed. |
| M5 | SiteHeader navigation still has a "Pricing" entry alongside "Memberships" — both navigate to `/memberships`, so it's a redundant label not a broken link | Verified — `src/components/SiteHeader.tsx:141-153`. Not fixed. |

### Build / tooling

| ID | Issue | Status |
|---|---|---|
| B18 (new) | `package.json` build script runs `node scripts/compress-images.js && next build`, but the `scripts/` directory does NOT exist in the repository. Local `bun run build` would fail at the first step. Production Vercel builds use `vercel.json` buildCommand (`next build`), so production is unaffected. | Verified — `git ls-tree -r HEAD --name-only \| grep "^scripts/"` returns nothing. Not fixed. |

### Accepted as design decisions

| ID | Item | Reason |
|---|---|---|
| B16 | Recharts (~600 KB) is in `dependencies` even though it's lazy-loaded | Acceptable — bundler code-splits it out of the initial bundle |
| B17 | Framer Motion animations are disabled (components render without animation) | Acceptable — owner decision to avoid layout jank |

---

## 7. Current Project Workflow

1. **Owner or reviewer opens a task** with a Task ID (e.g.
   `MH-DOC-001`) and a clear scope.
2. **Implementation agent** (GML or delegated sub-agent) reads
   `AGENTS.md` and `PROJECT_CONTEXT.md`, then inspects the actual
   source code before changing anything.
3. Agent implements → runs `tsc --noEmit` + `bun run lint` + smoke
   tests → updates docs (`README`, `PROGRESS`, `DEVELOPER_GUIDE`,
   `QA_CHECKLIST`, `SECURITY` as applicable).
4. Agent files a final report (template in `AGENTS.md` §9) including
   every discrepancy found and every assumption made.
5. **Human supervisor** reviews the diff, asks questions if needed,
   approves or rejects.
6. Approved changes are pushed to `origin` and the supervisor is
   notified for production deployment (Vercel).
7. After deployment, the supervisor updates `PROGRESS.md` to mark
   the work done.

---

## 8. Source-of-Truth Hierarchy (Reinforced)

When sources conflict, the higher source wins:

1. **Actual source code and configuration** (`src/**`, `next.config.ts`,
   `tsconfig.json`, `package.json`, `vercel.json`).
2. **Database migrations / schema** (`supabase/migrations/*.sql`).
3. **Verified tests and QA evidence** (`QA_CHECKLIST.md`, manual smoke
   tests recorded in commit messages).
4. **Documentation** (`README.md`, `DEVELOPER_GUIDE.md`, `PROGRESS.md`,
   `AGENTS.md`, this file, `SECURITY.md`).

Documentation is a derivative. If it disagrees with code, the code wins
and the documentation is updated in the same PR.

---

## 9. Current Agent / Supervisor Model

| Role | Who | Authority |
|---|---|---|
| Project Owner / Human Supervisor | `muscleshubfit@gmail.com` (Ahmed) | Final say on every change. Approves features, fixes, schema changes, security changes, deploys. |
| Technical Reviewer | ChatGPT (OpenAI) — invoked by the owner when a second pair of eyes is needed on architecture, security, or trade-offs. | Reviews proposals, suggests alternatives, flags risks. Does NOT commit code directly. |
| Implementation Agent | GML (this agent) + any sub-agent it delegates to. | Writes code, runs tests, updates docs, pushes commits. Every change must be reviewed/approved by the human supervisor before going to production. |

Agents are **implementers, not autonomous product owners**. They do
not unilaterally decide to add features, change the stack, expose
endpoints, or modify production data. See `AGENTS.md` for the full
operating rules.

---

## 10. Pointers

- **Operating rules for agents:** `AGENTS.md`
- **Security policy:** `SECURITY.md`
- **License:** `LICENSE` (proprietary, all rights reserved)
- **User-facing summary:** `README.md`
- **Developer onboarding:** `DEVELOPER_GUIDE.md`
- **Feature / bug tracker:** `PROGRESS.md`
- **QA evidence:** `QA_CHECKLIST.md`
- **Change log (per-agent worklog):** `worklog.md`
