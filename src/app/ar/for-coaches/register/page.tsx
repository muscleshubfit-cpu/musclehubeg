"use client";

/**
 * AR MIRROR of /for-coaches/register — same bilingual coach-registration
 * form (useI18n URL-first renders the Arabic content under /ar/*).
 * Indexable Arabic URL with Arabic-first metadata (see ./layout.tsx).
 */
export { default } from "@/app/for-coaches/register/page";
