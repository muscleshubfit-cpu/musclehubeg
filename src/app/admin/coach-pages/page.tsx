"use client";

import { AdminCoachPagesView } from "@/components/views/AdminCoachPagesView";

/**
 * ADMIN — COACH PAGES REVIEW surface (0046). Lives under the /admin
 * layout (noindex + AdminGate). The view itself is the whole page.
 */
export default function Page() {
  return <AdminCoachPagesView />;
}
