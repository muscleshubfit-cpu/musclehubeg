# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env.local
.env.*.local

# duplicate lockfile — we use bun.lock (Vercel runs `bun install`)
package-lock.json
yarn.lock
pnpm-lock.yaml

# vercel
.vercel

# typescript
*.tsbuildinfo

# scripts logs
*.log

# Original cloned repo (kept locally for reference, NOT pushed to GitHub)
/repo/
/tool-results/
# Everything under /scripts is local tooling/logs EXCEPT the two GHA
# runners, which MUST be versioned (workflows depend on them):
#   scripts/blog-runner     → blog pipeline v2 (generate-blog-post.yml)
#   scripts/ai-jobs-runner  → whole-AI-system queue worker (process-ai-jobs.yml)
/scripts/*
!/scripts/blog-runner/
!/scripts/ai-jobs-runner/
/skills/
/tests/
/examples/
/.zscripts/
/dev.log
/server.log
/download/
/prisma/
/mini-services/

# Database artifacts
/db/
*.db
.env*
.z-ai-config
.env.production

# Junk files (screenshots, uploads, temp images)
/upload/
/Screenshot_*.jpg
/Screenshot_*.png
/file_*.png
/.claude/
/Caddyfile
